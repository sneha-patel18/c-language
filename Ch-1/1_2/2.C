#include<stdio.h>
#include<conio.h>

main()
{
   int x,y,ans;
   clrscr();

   printf("(x+y)2 \t:");
   printf("enter a number to apply in an formula:");
   scanf("%d",x);
   printf("enter second number);
   scanf("%d",y);
   ans =(x*x)+(2*x*y)+(y*y);
   ans("")

   getch();
}
